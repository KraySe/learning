// Undefined -> Se asigna una variable pero no se le ha declarado ningún valor, variable global, valor indefinido.
// Null -> Es un valor de asignación que representa ausencia de valor. Tipo de valor object
let mount = null;
let myVar;
console.log(mount, myVar);
console.log(typeof mount, typeof myVar);
